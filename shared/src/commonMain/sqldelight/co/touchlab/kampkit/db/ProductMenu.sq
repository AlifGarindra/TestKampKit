CREATE TABLE ProductMenu (
rank INTEGER AS Int NOT NULL,
code TEXT NOT NULL PRIMARY KEY,
name TEXT NOT NULL
);

selectAll:
SELECT * FROM ProductMenu;

insertProductMenu:
INSERT OR IGNORE INTO ProductMenu(rank, code, name)
VALUES(?, ?, ?);